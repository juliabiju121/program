CREATE TABLE Store(
 ORDER_NO INT PRIMARY KEY,
  CODE VARCHAR(20),
  ITEM CHAR(20),
  QUANTITY  INT ,
  PRICE INT,
  DISCOUNT VARCHAR(20),
  MRP INT
  );
  INSERT INTO STORE VALUES
  (001,'SAKS1','PEN',20,100,'5%',1000),
  (002,'MASK1','BOOK',11,548,'5%',1000),
  (003,'SSAA1','PENCIL',15,356,'5%',1000);
  SELECT * FROM STORE;
  SELECT MOD(PRICE,9) AS REM FROM STORE ;
  SELECT POWER(PRICE,2) AS POW FROM STORE;
  SELECT ROUND(PRICE DIV 7) AS ROUND FROM STORE
  
  DROP TABLE STORE